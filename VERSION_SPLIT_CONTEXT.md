# 版本分组上下文

用户要求：
- 将旧版三端分到 `V1` 文件夹。
- 将新版整合项目分到 `V2` 文件夹。

计划：
- 停止相关本地服务：5174、5175、5188、5190。
- 创建桌面 `V1`、`V2`。
- 移动：
  - `光点小程序C端`、`光点小程序B端`、`光点小程序后台` -> `V1`
  - `光点新版完整项目` -> `V2`
- 重新启动 V2 的 5190 预览服务。
